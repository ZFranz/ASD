#include <stdio.h>

using namespace std;

int periodoNaive(string s) {
	int n = s.length;
	for (int p = 1; p <= n; p++) {
		string s1 = s.substring(0, n-p);
	}
}

int main() {
	
	
	return 0;
}
